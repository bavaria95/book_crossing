    <ul class="collection with-header">
        <li class="collection-header"><h3><b>Books on <i>Shelf</i></b></h3></li>
        <% @shelf.each do |book| %>
            <li class="collection-item"><div><h4><b><%= book.title %></b>, <%= book.author %></h4><a href="#" class="secondary-content"><i class="material-icons">send</i></a></div></li>
        <% end %>
</ul>
